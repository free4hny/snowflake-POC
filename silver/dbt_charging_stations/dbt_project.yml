name: 'ev_charging_dbt'
version: '1.0.0'
profile: 'ev_charging_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  ev_charging_dbt:
    silver:
      +materialized: table
    gold:
      +materialized: table
